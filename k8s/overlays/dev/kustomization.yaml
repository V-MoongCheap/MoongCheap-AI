apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Namespace example from the Cloud V2 design; the GitOps draft uses moongcheap-dev.
# Infrastructure confirms the namespace and provisions matching Secrets/PVCs.
namespace: ai

labels:
  - pairs:
      app.kubernetes.io/instance: ai-develop
    includeSelectors: false

# Keep the image/address/artifact placeholders and suspend=true until configured.
