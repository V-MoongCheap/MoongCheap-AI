apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Cloud V2 namespace. Infrastructure provisions it and the external dependencies.
namespace: ai

labels:
  - pairs:
      app.kubernetes.io/instance: ai-develop
    includeSelectors: false

# Keep the image/address/artifact placeholders and suspend=true until configured.
